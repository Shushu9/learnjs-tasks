// Вопрос об "if"
// важность: 5
// Какие из перечисленных ниже alert выполнятся?

// Какие конкретно значения будут результатами выражений в условиях if(...)?

// if (-1 || 0) alert( 'first' ); результат -1 = true. условие выполнится
// if (-1 && 0) alert( 'second' ); результат 0 = false. условие не выполнится
// if (null || -1 && 1) alert( 'third' ); результат 1 = true. условие выполнится